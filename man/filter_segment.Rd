% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter.R
\name{filter_segment}
\alias{filter_segment}
\title{Filter segmentation results}
\usage{
filter_segment(input, filter_words, keep_na = TRUE, unit = 50)
}
\arguments{
\item{input}{A character vector or a list of character vectors.}

\item{filter_words}{A character vector of words to remove.}

\item{keep_na}{Whether to keep \code{NA} values in the returned result. The
default \code{TRUE} matches \code{jiebaR::filter_segment()}.}

\item{unit}{\link{Deprecated} Unused compatibility argument retained with
\code{jiebaR::filter_segment()}.}
}
\value{
An object with the same shape as \code{input}, with matching words
removed.
}
\description{
Remove selected words from a segmented character vector or from each element
of a list of segmented character vectors.
}
\details{
This is a modern reimplementation of \code{jiebaR::filter_segment()} with the
same core filtering behavior under the default settings.

In the reproducible benchmark, this version is about 110x to 140x faster
than \code{jiebaR::filter_segment()} on the tested workloads.
}
\examples{
filter_segment(c("abc", "def", " ", "."), c("abc"))
filter_segment(c("a", NA, "b", "a"), c("b"), keep_na = FALSE)
filter_segment(list(c("我", "是", "测试"), c("测试", "文本", "我")), "我")
}
