% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/user-dict.R
\name{import_cidian}
\alias{import_cidian}
\title{Import an input-method dictionary into a worker}
\usage{
import_cidian(worker, path, tag = NULL)
}
\arguments{
\item{worker}{A \code{jieba_worker} object to update in place.}

\item{path}{A path to a supported input-method dictionary file.}

\item{tag}{An optional single non-empty character string assigned to every
imported word. \code{NULL} leaves the tag empty.}
}
\value{
\code{NULL}, invisibly. The supplied \code{worker} is modified in place.
}
\description{
Parse a supported Chinese input-method dictionary with the Rust \code{cidian}
parser and add all parsed words to an existing \code{jieba_worker}. The file
extension selects the parser automatically. Supported formats are \code{.scel},
\code{.qcel}, \code{.qpyd}, \code{.bdict}, and \code{.bcd}.
}
\details{
The source dictionary's coding and weight fields are ignored. Imported words
are added without an explicit frequency, so \code{jieba-rs} infers each
frequency from the current dictionary. The same \code{tag} is assigned to every
imported word. By default, imported words have no tag (\code{tag = NULL}).
}
\examples{
\dontrun{
cutter <- worker()
import_cidian(cutter, "dictionary.scel")
segment("词库中的词语", cutter)
}
}
