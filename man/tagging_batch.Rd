% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tagging.R
\name{tagging_batch}
\alias{tagging_batch}
\title{Tag a batch of strings}
\usage{
tagging_batch(
  texts,
  jiebar,
  format = c("vector", "data.frame", "legacy"),
  batch = c("list", "data.frame", "flatten")
)
}
\arguments{
\item{texts}{A non-empty character vector to tag.}

\item{jiebar}{A \code{jieba_worker} object created with \code{worker(type = "tag")}.}

\item{format}{Output format for each single tagged result. Must be one of
\code{"vector"}, \code{"data.frame"}, or \code{"legacy"}.}

\item{batch}{Aggregation mode. Must be one of \code{"list"}, \code{"data.frame"}, or
\code{"flatten"}.}
}
\value{
Tagging results in the requested format.
}
\description{
Convenience wrapper around \code{\link[=tagging]{tagging()}} for multi-string input. When \code{batch}
is not supplied, \code{tagging_batch()} always returns list output.
}
\details{
\code{tagging_batch()} is a convenience wrapper for explicit multi-string input.
The returned object depends on both \code{format} and \code{batch}:
\itemize{
\item \code{batch = "list"}: returns one single-string tagging result per input
string.
\item \code{batch = "data.frame"}: requires \code{format = "data.frame"}; combines all
rows and adds \code{doc_id}.
\item \code{batch = "flatten"}: requires \code{format = "vector"} or \code{"legacy"};
concatenates the individual results into one named character vector.
}
}
\examples{
tagger <- worker(type = "tag")
texts <- c("这是一个测试", "再来一次")
tagging_batch(texts, tagger)
tagging_batch(texts, tagger, format = "legacy", batch = "flatten")
}
